# TrustAgency 生产环境配置
# ⚠️ 注意：此文件包含敏感信息，绝不应该提交到 Git！
# 使用方法：docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d

# ==================== 应用配置 ====================
ENVIRONMENT=production
DEBUG=False

# ==================== 数据库配置 ====================
# PostgreSQL 密码（应该使用强随机密码）
# 示例：生成强密码的方法: openssl rand -base64 32
DB_PASSWORD=change-me-to-strong-password

# ==================== 安全配置 ====================
# JWT 密钥（应该使用强随机值）
# 生成方法: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-production-secret-key-change-this

# ==================== 其他配置 ====================
# 这些值在 .env.example 中有默认值，在生产环境可以覆盖
# OPENAI_API_KEY=sk-your-production-api-key
# REDIS_PASSWORD=your-redis-password
