# TrustAgency 生产环境配置 (SQLite版本)
# ⚠️ 注意：此文件包含敏感信息，绝不应该提交到 Git！
# 
# 使用方法：
# 1. 复制本文件：cp .env.prod.example .env.prod
# 2. 生成 SECRET_KEY：python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# 3. 编辑 .env.prod 并替换下面的占位符
# 4. 启动容器：docker-compose --env-file .env.prod -f docker-compose.prod.yml up -d

# ==================== 应用配置 ====================
ENVIRONMENT=production
DEBUG=False

# ==================== 数据库配置 ====================
# SQLite 配置（生产环境使用持久化卷存储）
# 数据库文件路径：/app/data/trustagency.db（通过Docker卷持久化）
DATABASE_URL=sqlite:////app/data/trustagency.db

# ==================== 安全配置 ====================
# JWT 密钥（应该使用强随机值）
# ⚠️ 必须修改！不要使用默认值！
# 
# 生成方法: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# 示例: SG1234567890abcdefghijklmnopqrst
#
# 重要：
# - 长度应该 >= 32 字符
# - 应该包含大小写、数字和特殊字符
# - 不要与其他密钥相同
# - 改变此值会导致所有现有 token 失效（用户需要重新登录）
SECRET_KEY=your-production-secret-key-change-this-NOW

# ==================== API配置 ====================
API_HOST=0.0.0.0
API_PORT=8001

# ==================== 日志配置 ====================
LOG_LEVEL=INFO
LOG_FORMAT=json

